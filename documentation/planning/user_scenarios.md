
# 👥 Итоговые пользовательские сценарии (MVP)

## 0) Общие допущения

- **Интерфейс**: Telegram-бот (кнопки/команды)
- **Время**: хранится в UTC, показывается в локали города
- **Оплата в MVP**: ручная отметка игроком («Оплатил») и обзор статусов у организатора
- **Отмечать оплату** можно только после начала игры (`now() >= startsAt`)

## 1) Первичное взаимодействие / онбординг

**Когда**: первый запуск бота

**Поток**:
/start → приветствие
↓
Выбор роли: Игрок / Организатор / Профи-игрок (опц.)
↓
Создаётся User(tgId, name, role)

**Post-условия**: пользователь зарегистрирован, запущен профиль соответствующей роли

## 2 Сценарии Игрока

### 2.1 Регистрация игрока

**Когда**: выбрана роль «Игрок»

**Шаги**:
1. Подтверждение имени (или ввод нового)
2. Самооценка уровня: Новичок / Средний / Продвинутый
3. Вопрос про «своего» организатора: Да/Нет

**Результат**: `User.levelTag` сохранён

### 2.2 Привязка к организатору

**Когда**: игрок указал, что у него есть «свой» организатор, или инициировал смену

**Шаги**:
1. Бот показывает список активных организаторов
2. Игрок выбирает одного или отмечает «моего нет в списке»

**Результат**:
- Выбран — создаётся связь `playerOrganizer(playerId, organizerId)`
- Нет в списке — фиксируется `pending_organizer_request`

### 2.3 Поиск и выбор игры

**Триггеры**: `/games` или кнопка «Подобрать игры», либо push-уведомление о подходящей игре

**Шаги**:
1. Бот показывает ближайшие open игры по уровню/городу
2. Игрок открывает карточку игры → «Записаться»

### 2.4 Запись на игру (join)

**Условия**: `Game.status='open'`, `startsAt > now()`

**Шаги**:
1. Игрок жмёт «Записаться»
2. Система проверяет вместимость:
   - если мест достаточно → `confirmed`
   - иначе → `waitlisted`

**Результат**: подтверждение статуса + появление игры в `/my`

### 2.5 Автопромоушен из листа ожидания

**Триггер**: другой игрок отменил запись или организатор освободил место

**Действия системы**: первый в waitlist переводится в `confirmed` → бот присылает уведомление

### 2.6 Отмена участия (leave)

**Триггеры**: кнопка «Отменить» в карточке игры или `/leave <id>`

**Шаги**:
1. Подтверждение → статус регистрации `canceled`
2. Система пытается промоутить первого из waitlist

**Итог**: игроку — подтверждение, «моя запись отменена»

### 2.7 Напоминания об игре

- **T-24 ч**: сообщение «Игра завтра в HH:MM на [площадке]…» + (опц.) кнопки «Иду/Не смогу»
- **T-2 ч**: сообщение «Игра через 2 часа…»

### 2.8 Оплата (после начала игры)

**Правило**: отметить оплату можно не раньше `startsAt`

**Потоки**:
- **До начала**: цена видна, но кнопка «Оплатил» не активна, подсказка: «Оплата откроется с начала игры»
- **В момент `startsAt`**: бот присылает сообщение с активной кнопкой «Оплатил»
- **Игрок жмёт «Оплатил»** → статус `paymentStatus='paid'`, `paymentMarkedAt=now()` → бот подтверждает и уведомляет организатора
- **Попытка раньше старта**: отказ + подсказка

### 2.9 Просмотр своих игр

**Команда**: `/my`

**Содержимое**: ближайшие и активные записи со статусами участия (`confirmed`/`waitlisted`) и оплаты (💰/❌)

**Действия**: перейти в карточку → отменить участие (если нужно) → (после `startsAt`) отметить оплату

## 3) Сценарии Организатора

### 3.1 Регистрация организатора

**Когда**: выбрана роль «Организатор»

**Шаги**:
1. Имя, контакт, краткое описание группы (опц.)

**Результат**: `Organizer(profile)` создан, роль закреплена

### 3.2 Создание игры

**Команда**: `/newgame`

**Шаги (мастер)**:
1. Дата (до 3 дней вперёд), время начала, длительность (по умолчанию 2 ч)
2. Площадка (из списка Venue или текст)
3. Целевой уровень (тег), лимит мест (≤14)
4. Цена/описание (текст)

**Результат**: `GameCreated(status='open')`, планируются напоминания T-24 и T-2

### 3.3 Управление набором

**Команды**:
- `/players <gameId>` — список `confirmed` / `waitlisted` / `canceled` + оплата (💰/❌)
- В карточке: удалить участника, добавить участника, закрыть набор
- `/close <gameId>` — новые записи запрещены

### 3.4 Напоминания и оплаты

- **До старта**: кнопка «Напомнить об оплате» недоступна
- **После старта (`startsAt`)**:
  - `/payments <gameId>` — список игроков с оплатами
  - «Напомнить неоплатившим» — массовая рассылка
  - каждый игрок может нажать «Оплатил» в персональном сообщении

**Итог**: организатор видит актуальные статусы оплат

### 3.5 Сообщение участникам

**Команда**: `/message <gameId>` → текст → бот рассылает всем `confirmed` (и опц. `waitlisted`)

### 3.6 Завершение / отчёт

**Команда**: `/report <gameId>`

**Содержимое**: кол-во записанных, пришедших (если отмечаете руками), оплативших / не оплативших

## 4) Сценарии системы (Policies / фоновые процессы)

| Policy / процесс | Триггер | Действие |
|------------------|---------|----------|
| Schedule T-24 | GameCreated | поставить задачу на напоминание за 24 ч |
| Schedule T-2 | GameCreated | поставить задачу на напоминание за 2 ч |
| Send T-24 | окно `now()+24h ±5m` | уведомить всех `confirmed` |
| Send T-2 | окно `now()+2h ±5m` | уведомить всех `confirmed` |
| Open Payment Window | `now() == startsAt` | разослать actionable «Оплатил» |
| Reject Early Payment | попытка оплаты до `startsAt` | отказ + подсказка |
| Promote Waitlisted | RegistrationCanceled или снятие слота | первый из waitlist → `confirmed` + уведомление |
| Close Registration on Status | GameClosed | блокировать join/leave (кроме организаторских действий) |